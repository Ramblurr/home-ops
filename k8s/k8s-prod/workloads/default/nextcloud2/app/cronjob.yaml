---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.26.1-standalone-strict/cronjob-batch-v1.json
apiVersion: batch/v1
kind: CronJob
metadata:
  name: &name nextcloud2-cron
  labels: &labels
    app.kubernetes.io/name: nextcloud2
    app.kubernetes.io/instance: nextcloud2
spec:
  schedule: "*/5 * * * *"
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 2
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      labels: *labels
    spec:
      template:
        metadata:
          labels: *labels
        spec:
          restartPolicy: Never
          containers:
            - name: *name
              image: nextcloud:26.0.1-fpm-alpine
              command:
                - curl
              args:
                - --silent
                - --show-error
                - https://nextcloud2.${SECRET_DOMAIN_INTERNAL}/cron.php
