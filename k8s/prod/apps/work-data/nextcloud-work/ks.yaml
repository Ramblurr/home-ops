---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: &app nextcloud-work
  namespace: flux-system
spec:
  targetNamespace: &ns work-data
  dependsOn:
    - name: cert-manager-issuers
    - name: app-db
  path: ./k8s/prod/apps/work-data/nextcloud-work/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
  #healthChecks:
  #  - apiVersion: helm.toolkit.fluxcd.io/v2beta2
  #    kind: HelmRelease
  #    name: nextcloud-work
  #    namespace: *ns
  interval: 30m
  retryInterval: 1m
  timeout: 3m
  postBuild:
    substitute:
      APP: *app
      APP_NS: *ns
      NC_DOMAIN: data.${SECRET_WORK_DOMAIN}
      VOLSYNC_CAPACITY: 5Gi
