---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config-0
  namespace: downloads
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: "${DEFAULT_STORAGE_CLASS}"
